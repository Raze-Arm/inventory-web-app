kind: ExternalService
name: mega-electric-web-app
spec:
  image:  razear/mega-electric-web-app:${SHA}
  env:
    - name: REACT_APP_HOST
      secret: backend-api

  port_mapping:
    - port: 3000
      target_port: 3000
  resources:
    memory: 200Mi